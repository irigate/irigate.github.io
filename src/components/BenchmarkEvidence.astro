<div class="evidence" aria-label="Measured identical-context Context7 benchmark evidence">
  <div>
    <span>5 clients</span>
    <strong>80% fewer</strong>
    <p>Context7 instances and 79.0% lower resident memory in the measured identical-context run.</p>
  </div>
  <div>
    <span>20 clients</span>
    <strong>95% fewer</strong>
    <p>Context7 instances and 94.7% lower resident memory in the measured identical-context run.</p>
  </div>
  <div>
    <span>Latency</span>
    <strong>No claim</strong>
    <p>Throttled calls do not establish a latency improvement.</p>
  </div>
</div>

<style>
  .evidence {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: var(--space-4);
  }

  .evidence div {
    border-top: 3px solid var(--color-accent);
    padding: var(--space-4) 0 0;
  }

  .evidence span {
    color: var(--color-text-muted);
    font-family: var(--font-mono);
    font-size: 0.82rem;
  }

  .evidence strong {
    display: block;
    margin-top: var(--space-2);
    font-family: var(--font-display);
    font-size: clamp(1.7rem, 6vw, 3rem);
    line-height: 1.05;
  }

  .evidence p {
    margin-top: var(--space-2);
    color: var(--color-text-muted);
  }

  @media (max-width: 46rem) {
    .evidence {
      grid-template-columns: 1fr;
    }
  }
</style>
